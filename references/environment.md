# Environment

Python 3.11+ and PyYAML are required. LaTeX builds require Tectonic or TeX Live. Marked manuscripts additionally require `latexdiff`. Development validation uses pytest, Ruff, Mypy and build.
